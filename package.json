{
  "name": "fart-soundboard",
  "version": "1.0.0",
  "description": "The ultimate fart soundboard with audio effects",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 8888",
    "build": "echo 'No build step needed'",
    "cap:init": "npx cap init 'Fart Soundboard' com.fartboard.app --web-dir .",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "keywords": ["soundboard", "fart", "audio", "effects"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
